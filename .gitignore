# Env
.env
.env.local
*.env

# Python
__pycache__/
*.py[cod]
.venv/
venv/
evenv/
*.egg-info/
.pytest_cache/

# Node / Next.js
node_modules/
.next/
out/
frontend/.next/

# Data / models (populated in later steps)
backend/app/ingestion/raw_cache/
backend/app/models/checkpoints/
*.pt
*.ckpt
# ... but ship the trained serving checkpoints (~1 MB each) so teammates get
# a working forecast without training:
!backend/checkpoints/*.pt

# Training bundles / logs
training_cubes_*.zip
.train_*.log

# OS / editor
.DS_Store
.vscode/
.idea/

# Docker volumes are managed by Docker itself, nothing to ignore here
backend/data/
